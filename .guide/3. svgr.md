# SVGR

```
  npm i @svgr/cli rimraf -D
  "generate-icon": "svgr --icon --title-prop --replace-attr-values '#fff=currentColor' -d src/components/Icon src/assets/icons",
```

```
  npm i @rollup/plugin-babel -D
```
